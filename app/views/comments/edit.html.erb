<div class="comment clearfix space">
  <div class="container">
    <div class="row">
      <div class="col-md-12">

<h3>Edit Comments</h3>
<%= form_for([@movier, @comment]) do |form| %>
  <%= form.text_area :body, class: "form-control", rows: 5, requered: true %>
  <%= form.submit class: "btn btn-outline-success" %>

<% end %>
</div>
</div>
</div>
</div>
